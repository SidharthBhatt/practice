# Definition for singly-linked list.
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next
class Solution:
    def addTwoNumbers(self, x1, y1): 
        while (x1.next is not None and y1.next is not None):
            val = x1.val + y1.val 
            print(z)
            z = ListNode(val, None)
            try ()
            if (previous is not None)
            x1 = x1.next
            y1 = y1.next

        
        
